package CollectionFramework.Collection.List;

/**
 * Vector 和 ArrayList 的异同点
 * 相同点：动态扩充容，不需指定大小；底层通过数组实现，可对元素进行快速随机访问或遍历（查）；允许增删改插，但效率低于 Array
 * 异同点：
 *     ① Vector 线程安全，支持线程同步（Synchronized），效率、性能比 ArrayList 低
 *     ② ArrayList 线程不安全，仅支持线程异步，但效率比 Vector 高
 */
public class VectorClass {
}
